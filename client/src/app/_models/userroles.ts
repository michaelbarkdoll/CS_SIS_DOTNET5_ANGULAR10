export interface UserRoles {
    // role: string;    //AppRole object in C#; An SQL table; contains roleId and userId
    userId: number; // AppUser.ID
    roleId: number; //1 Member,2 Admin,3 Moderator
    // user: string;    //AppUser object in C#; An SQL table
    
}